<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="project.Yaadhum_Oorae.Activity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".driverdetails2"></activity>
        <activity android:name=".driverdetails1" />
        <activity android:name=".PersonalDetailsActivity"></activity>
        <activity android:name=".activity_proposal_accept" />
        <activity android:name=".activity_vendor" />
        <activity android:name=".Verfiyemail" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@drawable/bg_ui" />

        <activity android:name=".Signup" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>